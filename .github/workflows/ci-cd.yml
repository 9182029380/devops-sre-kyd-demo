name: CI/CD to GHCR

on:
  push:
    branches: [ main ]

permissions:
  contents: read
  packages: write

jobs:
  build-and-push:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout source
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          distribution: 'temurin'
          java-version: '21'

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: 9182029380
          password: ${{ secrets.GHCR_KYD }}

      - name: Build Docker image
        run: |
          IMAGE_NAME=ghcr.io/9182029380/spring-boot-k8s-app
          
          docker build -t ghcr.io/9182029380/sprig-boot-k8s-app:latest .
          docker tag ghcr.io/9182029380/sprig-boot-k8s-app:latest ghcr.io/9182029380/sprig-boot-k8s-app:${{ github.sha }}

      - name: Push to GHCR
        run: |
          IMAGE_NAME=ghcr.io/9182029380/spring-boot-k8s-app
          
          docker push ghcr.io/9182029380/sprig-boot-k8s-app:latest
          docker push ghcr.io/9182029380/sprig-boot-k8s-app:${{ github.sha }}
